# Runtime dependencies are declared in pyproject.toml [project.dependencies].
# This file exists for tooling that does not read pyproject.toml (e.g. Docker,
# some IDE setups). Keep it in sync with pyproject.toml.
.
