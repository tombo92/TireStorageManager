# TireStorageManager
TireStorage Manager helps car shops track customer tires in storage. Manage winter &amp; summer sets, search, import/export Excel, and keep data safe with backups. Runs on Windows without Python and supports multi-PC access via a shared SQLite database.
